# Screenshot shot-list (capture these, in order)

Rules: PNG or JPEG, 9:16 up to 2:3 for phones (e.g., 1080×1920), min 2 — ideally 8. Clean state first: fresh profile list is fine, light theme recommended; capture 3-4 in dark theme as bonus variety.

1. **Home / main screen** — status card visible ("Not connected"), floating connect button centered.
2. **Tunnel type picker** — open "Add profile" so DNSTT…Tor types are visible.
3. **Profile editor** — show protocol fields for DNSTT with resolver added (blur/mask real credentials!).
4. **DNS Scanner** — mid-scan with a few results found.
5. **Split tunneling (App selector)** — grid of installed apps (use a clean test device — no personal app names).
6. **Settings** — visible options incl. dark mode & bandwidth limiting.
7. **Dark mode home + connected state** — green status, traffic counters showing KB/s.
8. **Debug log sheet** — optional technical audience shot.

Masking rules before upload:
- Cover any real IPs, domains, ports, passwords (edit with an image tool).
- Never include other people's names/apps in split-tunneling screenshot.

Tablet 7" / 10": reuse two widest phone shots; Play accepts scaled.

Feature graphic `feature_graphic.png` is already generated for you.
App icon 512px = `imgs/icon.png`.
