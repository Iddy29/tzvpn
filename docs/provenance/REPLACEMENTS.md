# Replacement Registry — SlipNet-derived → VPN-TZ independent

Each entry records: replaced component, new location, independence basis, and
the evidence that behavior is preserved. Baseline reference: `main@779ec73`.

| Phase | Replaced | New implementation | Independence basis | Equivalence evidence |
|---|---|---|---|---|
| 1 | `data/export/ConfigExporter+ConfigImporter` logic (URI codecs, record codec, encrypted containers) | `com.vpntz.app.config.*` (ProfileDocument, ProfileRecordCodec, CommunityUriCodec, WireBase64, crypto/VaultCrypto, crypto/Envelopes, ConfigGateway, DeviceKeyCipher) | Re-designed from the frozen behavioral spec `WIRE_FORMAT.md`; new model taxonomy (immutable document + tolerant parse outcome), new error model, new vault container; no source structure copied | 130 JVM tests: 88-field golden vector, 200-doc property roundtrip, community URI tables, AEAD tamper/wrong-password, short-tail defaults, legacy `tzvpn://` aliases, Go cross-language fixture contract (`tzgate/internal/clientcfg/crosslang_test.go`), both-flavor compile+test runs |
| 1 | bundle encryption writer (app-side) | `vpntz-vault://` container (opt-in) + legacy-compatible envelope emitter | PBKDF2(700k)+AES-GCM with AAD context binding; legacy envelope re-derived from spec (framing [01\|salt16\|iv12\|ct‖tag], PBKDF2 600k) | deterministic framing vectors + wrong-password/tamper/truncate tests |
| 2 | domain layer Android decoupling: `TunnelType.isAvailable()` read flavor `BuildConfig` directly; `ResolverScannerRepository` exposed `android.content.Context` in its interface | domain stays pure JVM: `isAvailable(includeTor, includeNaive)` takes flags as parameters (callers supply per-flavor BuildConfig values); `loadCountryCidrRanges(countryCode)` / `generateCountryRangeIps(countryCode, count)` drop the Context param (impl uses its injected `@ApplicationContext`) | dependency-inversion per plan "use interfaces/ports where external dependencies are required"; zero behavior change — same flag semantics, same asset loading | domain invariant test suite (model + use-case, pure JVM): chain matrix, expiry/availability, enum fallbacks, byte formatting, scanner scoring, use-case fakes; both-flavor compile + full unit run |
| 3 | app-side network packet codecs inside `tunnel/*`: `IpPacketParser` (+ `Protocol`/`TcpFlags`/`TcpHeader`/`IpPacket`/`ConnectionKey`), `TcpPacketBuilder`, `DnsUtils`, `ProtocolSniffer`, and the pure TLS ClientHello fragmentation logic embedded in `SniFragmentForwarder` | `com.vpntz.app.network.*` (IpPacketParser, TcpPacketBuilder, DnsUtils, ProtocolSniffer, TlsPacketFragmenter). The `tunnel/*` files become thin typealias/delegating facades so all existing call sites (KotlinTunnelManager, TunnelConnection, the SOCKS/DoH bridges) resolve through the canonical kit; `SniFragmentForwarder` delegates all packet math to `TlsPacketFragmenter`. | Re-authored from the wire specs (RFC 791/8200 IP, RFC 793 TCP + RFC 1071 checksums, RFC 1035 DNS, RFC 8446 ClientHello/SNI) into one deterministic, pure-JVM, Android-free package; tolerant of malformed input (returns null/false instead of throwing); socket/threading logic stays in `SniFragmentForwarder`. | 66 JVM tests: 12 byte-for-byte equivalence tests (new `network` vs the original `tunnel` implementations — captured before the facades were switched over) plus vector-driven tests for parsing, building, DNS, sniffing and fragmentation (normal/malformed/boundary/protocol-specific/fragmentation); both-flavor compile + full unit run (187 tests total). |
| 4 | ad-hoc, scattered connection-lifecycle state handling in `service/VpnConnectionManager` (inline guards + direct `MutableStateFlow` writes on every connect/disconnect/error path) | `com.vpntz.app.service.VpnStateMachine` — an explicit, thread-safe state machine over the existing domain `ConnectionState` (Disconnected/Connecting/Connected/Disconnecting/Error). `VpnConnectionManager` now delegates its guards and transitions to it, and exposes the machine's `StateFlow` as the single source of truth for the connection state (still synced from the authoritative `VpnRepository` state). | No new dependencies; deterministic, lock-guarded transitions; guards against repeated connect while connecting/connected, repeated disconnect while disconnected/disconnecting, and cancellation/cleanup (Connecting→Disconnected); `Error` is recoverable and preserved through teardown; Android/service responsibilities (`VpnService`, foreground lifecycle, VPN interface, tunnel start/stop) remain in `VpnTzService`. | 17 JVM state-machine tests (successful connect/disconnect, connect failure + recovery, disconnect during connection, cancellation/cleanup, repeated connect rejection, repeated disconnect no-op, error preservation through teardown, authoritative sync); both-flavor compile + full unit run (204 tests total). |

Intentional divergences (documented, non-breaking):
- import "newer version" notice threshold = own emit constant (43) — fixes baseline's spurious self-import warning (it compared against 28 while emitting 43).
- `vpntz-enc://` single-profile payloads REQUIRE the device-key port; without it the line is skipped with a warning instead of crashing the import.
- hysteria2 `m`/`pinSHA256` params remain accepted-and-ignored (as in baseline).

Still on the derived path (NOT yet replaced — tracked for later phases):
`data/local/*`, `datastore`, `service/*` (the `VpnStateMachine` connection orchestration is independent as of phase 4; `VpnTzService`'s Android/`VpnService` boundary and its per-protocol tunnel wiring remain derived), `tunnel/*` (tunnel lifecycle/relay/bridge classes remain, but the packet codecs and the TLS ClientHello fragmentation math are now canonical in `com.vpntz.app.network`), `presentation/*`, `di/*` (except the two new providers), Go `cli/`, Go `tzgate/` (business logic), `docs/USER_GUIDE.md`. Domain models/use-cases/repositories are independent as of phase 2 (`data/repository/*` implementations still consume the derived storage/tunnel layers until later phases).
